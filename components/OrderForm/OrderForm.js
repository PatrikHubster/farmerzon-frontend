import React from 'react';
import axios from 'axios';
import { Cookies } from 'react-cookie';
import { handleAuthSSR } from '../../helper/auth/auth';

const cookies = new Cookies();

const OrderForm = (props) => {
    return (
        <div>
          Test
        </div>
    )
}

export default OrderForm;
